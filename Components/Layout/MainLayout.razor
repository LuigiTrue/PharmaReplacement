@inherits LayoutComponentBase
@implements IDisposable
@using Microsoft.FluentUI.AspNetCore.Components
@inject ThemeState ThemeState


<FluentDesignTheme Mode="@ThemeMode" />

<div class="layout-container @ThemeClass">

    <div class="page">

       @* <div class="sidebar">
            <NavMenu />
        </div>*@
        <NavMenuOutlaw></NavMenuOutlaw>
            
        

        <main>
           @* <header class="app-main-header sticky top-0 border-b border-slate-200 bg-white px-6 py-3 shadow-sm dark:border-slate-700 dark:bg-slate-900">
                <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
                    <nav class="flex flex-wrap items-center gap-2" aria-label="Ações do cabeçalho">
                        <a href="/Home" class="inline-flex items-center gap-2 rounded-md border border-slate-200 bg-slate-50 px-3 py-2 text-sm font-medium text-slate-700 shadow-sm transition-colors hover:bg-blue-50 hover:text-blue-700 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-200 dark:hover:bg-blue-950 dark:hover:text-blue-200">
                            <i class="bi bi-info-circle" aria-hidden="true"></i>
                            <span>Sobre</span>
                        </a>
                    </nav>
                </div>
            </header>*@

            <div class="body-content">
                @Body
            </div>

        <footer class="app-footer">
            © @DateTime.Now.Year RepyPharma • Desenvolvido por Luigi Paoli
        </footer>
        </main>
    </div>
</div>


<div id="blazor-error-ui">
    An unhandled error has occurred.
    <a href="" class="reload">Reload</a>
    <a class="dismiss">🗙</a>
</div>

@code {
    private DesignThemeModes ThemeMode => ThemeState.IsDarkMode
        ? DesignThemeModes.Dark
        : DesignThemeModes.Light;

    private string ThemeClass => ThemeState.IsDarkMode ? "theme-dark dark" : "theme-light";

    protected override void OnInitialized()
    {
        ThemeState.OnChange += OnThemeChanged;
    }

    private void OnThemeChanged()
    {
        _ = InvokeAsync(StateHasChanged);
    }

    public void Dispose()
    {
        ThemeState.OnChange -= OnThemeChanged;
    }


    private async Task AtualizarVersaoAsync()
    {
        // sua lógica real de atualização aqui
        await Task.CompletedTask;
    }
}
